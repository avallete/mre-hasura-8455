table:
  schema: public
  name: users
select_permissions:
  - role: public
    permission:
      columns:
        - id
      filter: {}
  - role: user
    permission:
      columns:
        - name
      filter: {}
